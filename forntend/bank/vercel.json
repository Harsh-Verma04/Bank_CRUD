{
  "version": 2,
  "buildCommand": "npm install && npm run build -- --configuration production",
  "outputDirectory": "forntend/bank/dist/bank",
  "framework": "angular",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
