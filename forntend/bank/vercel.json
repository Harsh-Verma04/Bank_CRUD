{
  "buildCommand": "npm install && npm run build -- --configuration production",
  "outputDirectory": "frontend/bank/dist/bank",
  "framework": "angular",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
