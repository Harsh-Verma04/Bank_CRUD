{
  "version": 2,
  "buildCommand": "cd frontend/bank && npm install && npm run build -- --configuration production",
  "outputDirectory": "frontend/bank/dist/bank",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
